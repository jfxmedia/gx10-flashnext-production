diff --git a/scripts/serve-intel-ar.sh b/scripts/serve-intel-ar.sh
index d6a2f7c..35f8bbd 100755
--- a/scripts/serve-intel-ar.sh
+++ b/scripts/serve-intel-ar.sh
@@ -21,11 +21,13 @@ TABLE_DIR="${TABLE_DIR:-/models/ple-table-fp8}"
 PORT="${PORT:-18300}"
 CTX="${CTX:-262144}"
 SEQS="${SEQS:-8}"
+BATCHED_TOKENS="${BATCHED_TOKENS:-8192}"
 GPU_MEM="${GPU_MEM:-0.85}"
 MTP="${MTP:-2}"
 PREWARM="${PREWARM:-1}"
 TOOL_PARSER="${TOOL_PARSER:-qwen3_coder}"
 EXTRA="${EXTRA:-}"
+KV_DTYPE="${KV_DTYPE:-auto}"
@@ -33,7 +35,7 @@ EXTRA="${EXTRA:-}"
 SPLIT='["vllm::unified_attention_with_output","vllm::unified_mla_attention_with_output","vllm::mamba_mixer2","vllm::mamba_mixer","vllm::short_conv","vllm::qwen3_8_flash_next_ple_short_conv","vllm::qwen3_8_flash_next_qsa_with_output","vllm::linear_attention","vllm::qwen_gdn_attention_core","vllm::qwen_gdn_attention_core_fused_norm_packed","vllm::sparse_attn_indexer","vllm::ple_mmap_lookup"]'
-CC="${CC:--cc.cudagraph_mode=PIECEWISE -cc.splitting_ops=$SPLIT}"
+CC="${CC:--cc.cudagraph_mode=${CUDAGRAPH_MODE:-PIECEWISE} -cc.splitting_ops=$SPLIT}"
@@ -70,6 +72,7 @@ docker run -d --name "$NAME" --restart unless-stopped \
   -e VLLM_PLE_MMAP_PREWARM="$PREWARM" -e VLLM_PLE_MMAP_PREFETCH="${PLE_PREFETCH:-0}" \
   -e VLLM_PLE_MMAP_MADV_RANDOM="${PLE_MADV_RANDOM:-0}" \
+  -e VLLM_DISABLE_EAGLE_BLOCK_DROP="${VLLM_DISABLE_EAGLE_BLOCK_DROP:-0}" \
   -e VLLM_HIT_DEBUG="${HIT_DEBUG:-0}" \
@@ -77,6 +80,7 @@ docker run -d --name "$NAME" --restart unless-stopped \
   -e VLLM_MARLIN_USE_ATOMIC_ADD=1 \
   -e VLLM_FP8_HYBRID="${FP8_HYBRID:-1}" \
+  -e VLLM_QSA_EXACT_TOPK="${VLLM_QSA_EXACT_TOPK:-0}" \
@@ -84,10 +88,10 @@ docker run -d --name "$NAME" --restart unless-stopped \
-    $PC_ARG --enable-chunked-prefill --max-num-batched-tokens 8192 \
+    $PC_ARG --enable-chunked-prefill --max-num-batched-tokens "$BATCHED_TOKENS" \
@@
-    --kv-cache-dtype auto \
+    --kv-cache-dtype "$KV_DTYPE" \
